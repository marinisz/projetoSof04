export default abstract class Usuario{
    async login(key: string, data: any) {
        const result = {}

        return result;
    }

    async cadastrar(data: any) {
        const result = {}

        return result;
    }
}
